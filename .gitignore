# Datos (pesados / inmutables, versionados por DOI Mendeley, no por git)
# Solo la carpeta de datos en la raíz (no src/instrument_ir/data/).
/data/
*.zip

# Salidas de experimentos: versionamos los RESULTADOS (métricas, runs, traces, reports),
# pero ignoramos cachés pesadas y binarios regenerables.
mlruns/
outputs/cache/
outputs/cache_test/
outputs/mlruns/
outputs/mlflow.db
outputs/verify/
models/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.env

# Caches de herramientas
.pytest_cache/
.dvc/cache
.ruff_cache/
.mypy_cache/

# SO / editor
.DS_Store
.idea/
.vscode/
